# Versions the published results were produced with.
# Newer minor versions are generally fine; ema-pytorch changed its EMA signature after 0.5.x,
# so pin that one if you hit a TypeError on EMA(...).
torch==2.3.1
numpy==1.24.3
scikit-learn==1.3.0
einops==0.8.0
ema-pytorch==0.5.1
scipy==1.10.1
